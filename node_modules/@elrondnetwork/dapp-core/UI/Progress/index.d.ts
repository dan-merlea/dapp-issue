import { ReactNode } from 'react';
import { WithClassnameType } from '../types';
export interface ProgressProps extends WithClassnameType {
    id: string;
    done: boolean;
    children: ReactNode;
    expiresIn?: number;
    progress: {
        startTime: number;
        endTime: number;
    };
}
export declare const Progress: ({ id, children, progress, done, expiresIn, className }: ProgressProps) => JSX.Element;
//# sourceMappingURL=index.d.ts.map