import { ReactNode } from 'react';
import { WithClassnameType } from '../../../types';
import { ComponentTypeWithChildren } from '../types';
export interface CustomToastPropsType extends WithClassnameType {
    onDelete: () => void;
    message?: string;
    messageComponent?: ReactNode;
    duration?: number;
    CustomCloseButton?: ComponentTypeWithChildren<{
        onClick?: () => void;
    }>;
}
export declare const CustomToast: ({ onDelete, message, messageComponent, duration, CustomCloseButton, className }: CustomToastPropsType) => JSX.Element;
//# sourceMappingURL=index.d.ts.map