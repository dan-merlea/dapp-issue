import React, { ReactNode } from 'react';
import { DappModalConfig } from '../dappModal.types';
import { WithClassnameType } from '../../types';
export interface DappModalPropsType extends WithClassnameType {
    id?: string;
    visible: boolean;
    onHide?: () => void;
    parentElement?: Element;
    children?: ReactNode;
    config?: DappModalConfig;
}
export declare const DappModal: ({ id, visible, onHide, parentElement, config, children, className }: DappModalPropsType) => React.ReactPortal | null;
//# sourceMappingURL=DappModal.d.ts.map