import { ReactNode } from 'react';
import { ActiveLedgerTransactionType, MultiSignTransactionType } from 'types';
import { WithClassnameType } from '../../types';
export interface SignStepType extends WithClassnameType {
    onSignTransaction: () => void;
    onPrev: () => void;
    handleClose: () => void;
    waitingForDevice: boolean;
    error: string | null;
    callbackRoute?: string;
    title?: ReactNode;
    currentStep: number;
    currentTransaction: ActiveLedgerTransactionType | null;
    allTransactions: MultiSignTransactionType[];
    isLastTransaction: boolean;
}
export declare const SignStep: ({ onSignTransaction, handleClose, onPrev, title, waitingForDevice, currentTransaction, error, allTransactions, isLastTransaction, currentStep, className }: SignStepType) => JSX.Element | null;
//# sourceMappingURL=SignStep.d.ts.map