import { ReactNode } from 'react';
import { WithClassnameType } from '../../types';
export interface LedgerConnectPropsType extends WithClassnameType {
    onClick: () => void;
    error: string;
    connectPageContent?: ReactNode;
}
export declare const LedgerConnect: ({ onClick, error, connectPageContent, className }: LedgerConnectPropsType) => JSX.Element;
//# sourceMappingURL=LedgerConnect.d.ts.map