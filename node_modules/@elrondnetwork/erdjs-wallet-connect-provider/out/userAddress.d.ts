import { IAddress } from "./interface";
export declare class UserAddress implements IAddress {
    private readonly value;
    private constructor();
    static fromBech32(value: string): UserAddress;
    bech32(): string;
}
